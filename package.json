{
  "name": "content",
  "version": "1.0.0",
  "description": "Entity dataset for search interface",
  "scripts": {
    "build": "node scripts/build-index.js",
    "generate": "node scripts/generate-entity.js",
    "generate-batch": "node scripts/generate-batch.js",
    "reconcile": "node scripts/reconcile.js"
  },
  "dependencies": {
    "openai": "^4.0.0"
  }
}
